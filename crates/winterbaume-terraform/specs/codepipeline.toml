# Auto-ingested by `tf-converter-codegen ingest codepipeline`.
# Hand-tuned: dropped json-blob fields (`stage`, `artifact_store`,
# `trigger`, `variable`, `tags_all`) so the converter reads them straight
# from instance.attributes; fixed `tags` from `string?` to `tags`.

[service]
name = "codepipeline"
crate_name = "winterbaume_codepipeline"
state_view_module = "views"

[[resource]]
type = "aws_codepipeline"
model = "PipelineTfModel"
view = "PipelineView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "name"
view = "name"
type = "string"
required = true

[[resource.field]]
hcl = "arn"
view = "arn"
type = "string?"

[[resource.field]]
hcl = "role_arn"
view = "role_arn"
type = "string?"

[[resource.field]]
hcl = "execution_mode"
view = "execution_mode"
type = "string?"

[[resource.field]]
hcl = "pipeline_type"
view = "pipeline_type"
type = "string?"

[[resource.field]]
hcl = "tags"
view = "tags"
type = "tags"
