[service]
name = "appfabric"
crate_name = "winterbaume_appfabric"
state_view_module = "views"

[[resource]]
type = "aws_appfabric_app_bundle"
model = "AppBundleTfModel"
view = "AppBundleView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "arn"
view = "arn"
type = "string?"

[[resource.field]]
hcl = "customer_managed_key_arn"
view = "customer_managed_key_arn"
type = "string?"

[[resource.field]]
hcl = "tags"
view = "tags"
type = "tags"

# ---------------------------------------------------------------------------
# aws_appfabric_app_authorization
# No state slot in winterbaume_appfabric; warning-only inject.
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_appfabric_app_authorization"
model = "AppAuthorizationTfModel"
view = "AppBundleView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "app"
view = "app"
type = "string"
required = true

[[resource.field]]
hcl = "app_bundle_arn"
view = "app_bundle_arn"
type = "string"
required = true

[[resource.field]]
hcl = "arn"
view = "arn"
type = "string?"

[[resource.field]]
hcl = "auth_type"
view = "auth_type"
type = "string?"

# ---------------------------------------------------------------------------
# aws_appfabric_app_authorization_connection
# No state slot in winterbaume_appfabric; warning-only inject.
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_appfabric_app_authorization_connection"
model = "AppAuthorizationConnectionTfModel"
view = "AppBundleView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "app_authorization_arn"
view = "app_authorization_arn"
type = "string"
required = true

[[resource.field]]
hcl = "app_bundle_arn"
view = "app_bundle_arn"
type = "string"
required = true

# ---------------------------------------------------------------------------
# aws_appfabric_ingestion
# No state slot in winterbaume_appfabric; warning-only inject.
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_appfabric_ingestion"
model = "IngestionTfModel"
view = "AppBundleView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "app"
view = "app"
type = "string"
required = true

[[resource.field]]
hcl = "app_bundle_arn"
view = "app_bundle_arn"
type = "string"
required = true

[[resource.field]]
hcl = "arn"
view = "arn"
type = "string?"

[[resource.field]]
hcl = "ingestion_type"
view = "ingestion_type"
type = "string?"

[[resource.field]]
hcl = "tenant_id"
view = "tenant_id"
type = "string?"

# ---------------------------------------------------------------------------
# aws_appfabric_ingestion_destination
# No state slot in winterbaume_appfabric; warning-only inject.
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_appfabric_ingestion_destination"
model = "IngestionDestinationTfModel"
view = "AppBundleView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "app_bundle_arn"
view = "app_bundle_arn"
type = "string"
required = true

[[resource.field]]
hcl = "ingestion_arn"
view = "ingestion_arn"
type = "string"
required = true

[[resource.field]]
hcl = "arn"
view = "arn"
type = "string?"
