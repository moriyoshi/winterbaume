# Auto-ingested by `tf-converter-codegen ingest iot`.
# Hand-tuned: split into one [[resource]] block per resource type
# (thing / thing_type / policy / certificate). Dropped nested-block
# fields (attributes map, properties block, ca_pem) — those are read
# straight from instance.attributes in the converter. ARN templates
# are hand-written in the converter.

[service]
name = "iot"
crate_name = "winterbaume_iot"
state_view_module = "views"

# ---------------------------------------------------------------------------
# aws_iot_thing
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_iot_thing"
model = "ThingTfModel"
view = "ThingView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "name"
view = "name"
type = "string"
required = true

[[resource.field]]
hcl = "arn"
view = "arn"
type = "string?"

[[resource.field]]
hcl = "thing_type_name"
view = "thing_type_name"
type = "string?"

[[resource.field]]
hcl = "default_client_id"
view = "default_client_id"
type = "string?"

[[resource.field]]
hcl = "version"
view = "version"
type = "i64"
default = 1

# ---------------------------------------------------------------------------
# aws_iot_thing_type
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_iot_thing_type"
model = "ThingTypeTfModel"
view = "ThingTypeView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "name"
view = "name"
type = "string"
required = true

[[resource.field]]
hcl = "arn"
view = "arn"
type = "string?"

[[resource.field]]
hcl = "id"
view = "id"
type = "string?"

[[resource.field]]
hcl = "deprecated"
view = "deprecated"
type = "bool"
default = false

# ---------------------------------------------------------------------------
# aws_iot_policy
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_iot_policy"
model = "IotPolicyTfModel"
view = "IotPolicyView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "name"
view = "name"
type = "string"
required = true

[[resource.field]]
hcl = "arn"
view = "arn"
type = "string?"

[[resource.field]]
hcl = "policy"
view = "policy"
type = "string?"

[[resource.field]]
hcl = "default_version_id"
view = "default_version_id"
type = "string?"

# ---------------------------------------------------------------------------
# aws_iot_certificate
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_iot_certificate"
model = "CertificateTfModel"
view = "CertificateView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "id"
view = "id"
type = "string?"

[[resource.field]]
hcl = "arn"
view = "arn"
type = "string?"

[[resource.field]]
hcl = "certificate_pem"
view = "certificate_pem"
type = "string?"

[[resource.field]]
hcl = "ca_certificate_id"
view = "ca_certificate_id"
type = "string?"

[[resource.field]]
hcl = "active"
view = "active"
type = "bool"
default = true

# ---------------------------------------------------------------------------
# aws_iot_authorizer (warning-only — no state slot in IoT view)
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_iot_authorizer"
model = "IotAuthorizerTfModel"
view = "AuthorizerView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "name"
view = "name"
type = "string"
required = true

[[resource.field]]
hcl = "arn"
view = "arn"
type = "string?"

[[resource.field]]
hcl = "authorizer_function_arn"
view = "authorizer_function_arn"
type = "string?"

[[resource.field]]
hcl = "signing_disabled"
view = "signing_disabled"
type = "bool"
default = false

[[resource.field]]
hcl = "status"
view = "status"
type = "string?"

[[resource.field]]
hcl = "token_key_name"
view = "token_key_name"
type = "string?"

[[resource.field]]
hcl = "enable_caching_for_http"
view = "enable_caching_for_http"
type = "bool"
default = false

# ---------------------------------------------------------------------------
# aws_iot_billing_group
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_iot_billing_group"
model = "IotBillingGroupTfModel"
view = "BillingGroupView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "name"
view = "name"
type = "string"
required = true

[[resource.field]]
hcl = "arn"
view = "arn"
type = "string?"

[[resource.field]]
hcl = "id"
view = "id"
type = "string?"

# ---------------------------------------------------------------------------
# aws_iot_ca_certificate
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_iot_ca_certificate"
model = "IotCaCertificateTfModel"
view = "CACertificateView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "id"
view = "id"
type = "string?"

[[resource.field]]
hcl = "arn"
view = "arn"
type = "string?"

[[resource.field]]
hcl = "ca_certificate_pem"
view = "ca_certificate_pem"
type = "string?"

[[resource.field]]
hcl = "active"
view = "active"
type = "bool"
default = true

[[resource.field]]
hcl = "allow_auto_registration"
view = "allow_auto_registration"
type = "bool"
default = false

[[resource.field]]
hcl = "certificate_mode"
view = "certificate_mode"
type = "string?"

# ---------------------------------------------------------------------------
# aws_iot_domain_configuration
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_iot_domain_configuration"
model = "IotDomainConfigurationTfModel"
view = "DomainConfigurationView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "name"
view = "name"
type = "string"
required = true

[[resource.field]]
hcl = "arn"
view = "arn"
type = "string?"

[[resource.field]]
hcl = "domain_name"
view = "domain_name"
type = "string?"

[[resource.field]]
hcl = "service_type"
view = "service_type"
type = "string?"

[[resource.field]]
hcl = "status"
view = "status"
type = "string?"

# ---------------------------------------------------------------------------
# aws_iot_event_configurations (warning-only — no state slot)
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_iot_event_configurations"
model = "IotEventConfigurationsTfModel"
view = "EventConfigurationsView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "id"
view = "id"
type = "string?"

# ---------------------------------------------------------------------------
# aws_iot_indexing_configuration
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_iot_indexing_configuration"
model = "IotIndexingConfigurationTfModel"
view = "IndexingConfigurationView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "id"
view = "id"
type = "string?"

# ---------------------------------------------------------------------------
# aws_iot_logging_options (warning-only — no state slot)
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_iot_logging_options"
model = "IotLoggingOptionsTfModel"
view = "LoggingOptionsView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "default_log_level"
view = "default_log_level"
type = "string?"

[[resource.field]]
hcl = "role_arn"
view = "role_arn"
type = "string?"

[[resource.field]]
hcl = "disable_all_logs"
view = "disable_all_logs"
type = "bool"
default = false

# ---------------------------------------------------------------------------
# aws_iot_policy_attachment (snapshot+mutate — attaches policy to target)
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_iot_policy_attachment"
model = "IotPolicyAttachmentTfModel"
view = "PolicyAttachmentView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "policy"
view = "policy"
type = "string"
required = true

[[resource.field]]
hcl = "target"
view = "target"
type = "string"
required = true

# ---------------------------------------------------------------------------
# aws_iot_provisioning_template (warning-only — no state slot)
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_iot_provisioning_template"
model = "IotProvisioningTemplateTfModel"
view = "ProvisioningTemplateView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "name"
view = "name"
type = "string"
required = true

[[resource.field]]
hcl = "arn"
view = "arn"
type = "string?"

[[resource.field]]
hcl = "description"
view = "description"
type = "string?"

[[resource.field]]
hcl = "enabled"
view = "enabled"
type = "bool"
default = true

[[resource.field]]
hcl = "provisioning_role_arn"
view = "provisioning_role_arn"
type = "string?"

[[resource.field]]
hcl = "template_body"
view = "template_body"
type = "string?"

[[resource.field]]
hcl = "default_version_id"
view = "default_version_id"
type = "i64"
default = 1

# ---------------------------------------------------------------------------
# aws_iot_role_alias
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_iot_role_alias"
model = "IotRoleAliasTfModel"
view = "RoleAliasView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "alias"
view = "alias"
type = "string"
required = true

[[resource.field]]
hcl = "arn"
view = "arn"
type = "string?"

[[resource.field]]
hcl = "role_arn"
view = "role_arn"
type = "string"
required = true

[[resource.field]]
hcl = "credential_duration"
view = "credential_duration"
type = "i64"
default = 3600

# ---------------------------------------------------------------------------
# aws_iot_thing_group
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_iot_thing_group"
model = "IotThingGroupTfModel"
view = "ThingGroupView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "name"
view = "name"
type = "string"
required = true

[[resource.field]]
hcl = "arn"
view = "arn"
type = "string?"

[[resource.field]]
hcl = "id"
view = "id"
type = "string?"

[[resource.field]]
hcl = "parent_group_name"
view = "parent_group_name"
type = "string?"

[[resource.field]]
hcl = "version"
view = "version"
type = "i64"
default = 1

# ---------------------------------------------------------------------------
# aws_iot_thing_group_membership (snapshot+mutate)
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_iot_thing_group_membership"
model = "IotThingGroupMembershipTfModel"
view = "ThingGroupMembershipView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "thing_name"
view = "thing_name"
type = "string"
required = true

[[resource.field]]
hcl = "thing_group_name"
view = "thing_group_name"
type = "string"
required = true

[[resource.field]]
hcl = "override_dynamic_group"
view = "override_dynamic_group"
type = "bool"
default = false

# ---------------------------------------------------------------------------
# aws_iot_thing_principal_attachment (snapshot+mutate)
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_iot_thing_principal_attachment"
model = "IotThingPrincipalAttachmentTfModel"
view = "ThingPrincipalAttachmentView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "thing"
view = "thing"
type = "string"
required = true

[[resource.field]]
hcl = "principal"
view = "principal"
type = "string"
required = true

# ---------------------------------------------------------------------------
# aws_iot_topic_rule
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_iot_topic_rule"
model = "IotTopicRuleTfModel"
view = "TopicRuleView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "name"
view = "name"
type = "string"
required = true

[[resource.field]]
hcl = "arn"
view = "arn"
type = "string?"

[[resource.field]]
hcl = "description"
view = "description"
type = "string?"

[[resource.field]]
hcl = "enabled"
view = "enabled"
type = "bool"
default = true

[[resource.field]]
hcl = "sql"
view = "sql"
type = "string"
required = true

[[resource.field]]
hcl = "sql_version"
view = "sql_version"
type = "string?"

# ---------------------------------------------------------------------------
# aws_iot_topic_rule_destination (warning-only — no state slot)
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_iot_topic_rule_destination"
model = "IotTopicRuleDestinationTfModel"
view = "TopicRuleDestinationView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "arn"
view = "arn"
type = "string?"

[[resource.field]]
hcl = "enabled"
view = "enabled"
type = "bool"
default = true
