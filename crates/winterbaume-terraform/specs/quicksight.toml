# Auto-ingested by `tf-converter-codegen ingest quicksight`.
# Hand-tuned: split into one [[resource]] block per resource type
# (data_source / group / user). Dropped Vec<T>/nested-block fields
# (credentials, parameters, permission, vpc_connection_properties,
# ssl_properties, tags_all). Those are read straight from
# instance.attributes in the converter. ARN templates and namespace
# fallback are wired up by hand.

[service]
name = "quicksight"
crate_name = "winterbaume_quicksight"
state_view_module = "views"

# ---------------------------------------------------------------------------
# aws_quicksight_data_source
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_quicksight_data_source"
model = "QuickSightDataSourceTfModel"
view = "QuickSightDataSourceView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "data_source_id"
view = "data_source_id"
type = "string"
required = true

[[resource.field]]
hcl = "name"
view = "name"
type = "string?"

[[resource.field]]
hcl = "arn"
view = "arn"
type = "string?"

[[resource.field]]
hcl = "type"
view = "ds_type"
type = "string?"

[[resource.field]]
hcl = "status"
view = "status"
type = "string?"

[[resource.field]]
hcl = "created_time"
view = "created_time"
type = "string?"

[[resource.field]]
hcl = "last_updated_time"
view = "last_updated_time"
type = "string?"

# ---------------------------------------------------------------------------
# aws_quicksight_group
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_quicksight_group"
model = "QuickSightGroupTfModel"
view = "QuickSightGroupView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "group_name"
view = "group_name"
type = "string"
required = true

[[resource.field]]
hcl = "namespace"
view = "namespace"
type = "string?"

[[resource.field]]
hcl = "arn"
view = "arn"
type = "string?"

[[resource.field]]
hcl = "description"
view = "description"
type = "string?"

[[resource.field]]
hcl = "principal_id"
view = "principal_id"
type = "string?"

# ---------------------------------------------------------------------------
# aws_quicksight_user
# ---------------------------------------------------------------------------

[[resource]]
type = "aws_quicksight_user"
model = "QuickSightUserTfModel"
view = "QuickSightUserView"

[resource.modes]
mode = "model_only"

[[resource.field]]
hcl = "user_name"
view = "user_name"
type = "string"
required = true

[[resource.field]]
hcl = "namespace"
view = "namespace"
type = "string?"

[[resource.field]]
hcl = "arn"
view = "arn"
type = "string?"

[[resource.field]]
hcl = "email"
view = "email"
type = "string?"

[[resource.field]]
hcl = "user_role"
view = "user_role"
type = "string?"

[[resource.field]]
hcl = "identity_type"
view = "identity_type"
type = "string?"

[[resource.field]]
hcl = "principal_id"
view = "principal_id"
type = "string?"
