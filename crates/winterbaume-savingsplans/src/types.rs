// Internal domain types for the Savings Plans service.
// Currently all domain types live in state.rs (PlanRecord) and views.rs (PlanRecordView).
// This module is reserved for shared types that span handler, state, and view layers.
