// Internal domain types for the CodeStar Notifications service.
// Currently all domain types live in state.rs; this module is reserved
// for future internal types that handlers and state may share.
