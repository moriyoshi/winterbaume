//! Domain type aliases and helpers for the Cognito Sync service.
//!
//! This module is intentionally minimal — the primary domain types
//! are in `state.rs` and the generated wire types are in `model.rs`.
