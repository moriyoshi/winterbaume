{
  "name": "winterbaume-docs",
  "private": true,
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "generate": "node scripts/generate-service-pages.mjs"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
